[package]
name = "context-vectors"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = {version = "0.21.0", features = ["flex", "dataset"] }
burn-helpers = { path = "../burn-helpers" }
tiktoken = "3.5.1"
