[package]
name = "calculate-loss"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.21.0", features = ["flex", "std", "train", "accelerate"] }
tiktoken = "4.1.2"
burn-helpers = { path = "../../burn-helpers" }
