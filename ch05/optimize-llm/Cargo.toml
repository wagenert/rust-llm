[package]
name = "optimize-llm"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.21.0", features = ["wgpu", "flex", "std", "train", "accelerate", "openblas-system"] }
tiktoken = "4.1.2"
burn-helpers = { path = "../../burn-helpers" }
train-helpers = { path = "../train-helpers" }
