[package]
name = "load-llm"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.21.0", features = ["std", "wgpu", "accelerate", "autodiff", "store"] }
burn-helpers = { path = "../../burn-helpers" }
