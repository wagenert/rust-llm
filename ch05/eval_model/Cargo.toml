[package]
name = "eval_model"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.21.0", features = ["flex", "wgpu", "std", "train", "accelerate"] }
tiktoken = { version = "4.1.2" }
burn-helpers = { path = "../../burn-helpers"}
