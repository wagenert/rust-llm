[package]
name = "multi-head-attention"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.21.0", features = ["flex"] }
burn-helpers = { version = "0.3.0", path = "../../burn-helpers" }
