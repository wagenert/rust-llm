[package]
name = "byte-pair-encoding"
version = "0.2.0"
edition = "2024"

[dependencies]
burn = { version = "0.21.0", features = ["wgpu", "dataset"] }
tiktoken = "3.5.1"
burn_helpers = { path = "../burn_helpers" }
