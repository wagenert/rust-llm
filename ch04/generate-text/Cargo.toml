[package]
name = "generate-text"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.21.0", features = ["wgpu", "std", "train"] }
tiktoken = "4.1.2"
llm-helpers = { path = "../llm-helpers"}
